// Placeholder manifest file.
// the installer will append this file to the app vendored assets here: vendor/assets/javascripts/spree/backend/all.js'

$(function() {
  $('.select2-min-len-4').select2({
    minimumInputLength: 4
  })
});
